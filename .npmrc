# Always install optional dependencies for all platforms.
# This ensures Linux native binaries (e.g. @rollup/rollup-linux-x64-gnu)
# are included in the lockfile even when generated on Windows/macOS.
optional=true
